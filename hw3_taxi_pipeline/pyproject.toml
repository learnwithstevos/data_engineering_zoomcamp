[project]
name = "hw3-taxi-pipeline"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "learnwithstevo", email = "296191432+learnwithstevos@users.noreply.github.com" }
]
requires-python = ">=3.14"
dependencies = [
    "dlt[duckdb,hub]>=1.29.1",
    "ibis-framework>=12.0.0",
    "marimo>=0.23.16",
    "pyarrow>=25.0.0",
]

[project.scripts]
hw3-taxi-pipeline = "hw3_taxi_pipeline:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
